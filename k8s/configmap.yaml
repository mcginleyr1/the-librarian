apiVersion: v1
kind: ConfigMap
metadata:
  name: librarian-config
  namespace: librarian
data:
  PHX_HOST: "librarian.tailf742b.ts.net"
  PHX_SERVER: "true"
  PORT: "4000"
  POOL_SIZE: "10"
  STORAGE_PATH: "/data/librarian"
  # Tailscale terminates TLS — disable Phoenix's internal force_ssl redirect
  DISABLE_FORCE_SSL: "true"
