apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: librarian

resources:
  - namespace.yaml
  - configmap.yaml
  - postgres.yaml
  - app.yaml
  - tailscale.yaml
  # secrets created via: just k-create-secrets <db-password>
